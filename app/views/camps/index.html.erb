<div class="row">
  <div class="small-7 columns">
    <!-- MAIN CONTENT GOES HERE -->
    <%= render :partial => "camps", locals: { status: "Upcoming", camps: @upcoming_camps }  unless @upcoming_camps.empty? %>
    <p>&nbsp;</p>
    <%= render :partial => "camps", locals: { status: "Past", camps: @past_camps } unless @past_camps.empty? %>
    <br />
    <%= link_to "Add a new camp", new_camp_path, class: "button tiny radius" %>
  </div>

  <div class="small-1 columns">
    &nbsp;
  </div>

  <div class="small-4 columns">
    <!-- SIDEBAR CONTENT GOES HERE -->
    <%= render :partial => "inactive_camps" %>
  </div>
</div>